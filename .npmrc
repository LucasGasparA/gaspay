# React Native / Expo não resolvem symlinks aninhados de forma confiável.
# Hoisting achatado é o layout suportado para Expo em monorepo pnpm.
node-linker=hoisted
strict-peer-dependencies=false
auto-install-peers=true
